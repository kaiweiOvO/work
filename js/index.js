window.onload = function() {
    let swiper = new Swiper('.swiper-container', {
        // effect: 'fade',
        loop: true,
        speed: 1000,
        autoplay: {
            delay: 3000
        }
    })
}